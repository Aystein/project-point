import { Trace } from "./Trace";

export class LassoInteraction extends Trace {
    constructor() {
        super();
    }

    

    render(renderer: THREE.WebGLRenderer, width: number, height: number) {

    }
}